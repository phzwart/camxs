#!/usr/bin/env python

import sys,os
import utils
if __name__ == "__main__":
    if len(sys.argv) == 3:
        eq_img      = sys.argv[1]
        mask_params = sys.argv[2]
        utils.mask_utils( eq_img, mask_params )
    else:
        print( " usage: \n    camxs.mask_gen image.npy mask.params  ")


